Flask>=3.0,<4
waitress>=3.0,<4
# Renders Thai/Lao receipts as images for the thermal printer.
Pillow>=10
# Windows ships no timezone database; zoneinfo needs this for Asia/Vientiane.
tzdata>=2024.1
# Windows USB printers go through the Windows spooler.
pywin32>=306; sys_platform == "win32"
