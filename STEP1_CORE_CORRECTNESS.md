# ขั้น 1 — ความถูกต้องของแกนระบบ (เงิน: ออเดอร์ → รับเงิน → คืนเงิน → ปิดกะ → รายงาน)

## หลักฐานก่อนเริ่ม
- ไฟล์ `round*_audit.py` ส่วนใหญ่ตรวจแค่ "มีข้อความนี้ในโค้ด" ไม่ได้รันระบบ จึงไม่นับเป็นหลักฐานว่าเงินตรง
- การทดสอบที่รันจริงมีแค่ `tests/test_runtime_regressions.py` และไม่มีเคสกระทบยอดเงินข้ามขั้นตอน

## ชุดทดสอบใหม่: `tests/test_money_reconciliation.py` (17 เคส)
ทุกเคสจบด้วย `assert_ledger()` ซึ่งตรวจว่า
1. ยอดรายการอาหาร = ยอดบิลที่บันทึก
2. เงินที่รับ (ไม่นับที่ถูกย้อน) = ยอดที่ต้องจ่ายของบิลนั้นพอดี
3. คืนเงินรวมไม่เกินยอดที่รับ
4. บิลค้างจ่ายต้องไม่มีรายการรับเงินค้างอยู่
5. รายงาน: ยอดขาย = ยอดรับเงิน, แยกช่องทางรวมกันได้เท่ากัน, ยอดคืนเงินและยอดสุทธิตรง

รันใน CI ทั้ง SQLite และ PostgreSQL 16

## บั๊กที่พบและแก้แล้ว
| # | ปัญหา | ผลในร้าน | การแก้ |
|---|---|---|---|
| 1 | รับเงินได้โดยไม่เปิดกะ | เงินไม่อยู่ในรายงานกะใดเลย ตรวจยอดไม่ได้ | ทุกช่องทางต้องมีกะเปิด (409 `shift_required`); หน้าชำระเงินมีปุ่มเปิดกะในตัว |
| 2 | คืนเงินบิลหลายช่องทาง ระบบตัดจากช่องทางแรกเสมอ | คืนเงินสดแต่ระบบตัดยอดโอน → ลิ้นชักขาด | API รับ `method`; หน้าจอมีช่องเลือกเมื่อบิลจ่ายหลายช่องทาง |
| 3 | คนหนึ่งรับเงินสด อีกคนเปิดบิลกลับ | ลิ้นชักคนแรกเกิน คนที่สองขาด | คำนวณแบบบัญชีคู่: รับเข้ากะไหนนับกะนั้น ย้อนในกะไหนหักกะนั้น |
| 4 | จับคู่เงินกับกะด้วยเวลาระดับวินาที | ปิด/เปิดกะในวินาทีเดียวกัน ยอดซ้ำสองกะ | เพิ่ม `payments.shift_id`; ข้อมูลเก่ายังใช้วิธีเดิม |
| 5 | SQLite ไม่มีการล็อกแถว | สองเครื่อง: รวมบิลเข้าบิลที่เพิ่งจ่าย → บิลที่จ่ายแล้วยอดเพิ่ม (พบ 4/5 รอบ) | `_for_update()` ใช้ `BEGIN IMMEDIATE` บน SQLite, `FOR UPDATE` บน Postgres; รวมบิลล็อกทั้งสองบิล |
| 6 | เน็ตหลุดหลังกดชำระ แล้วกดซ้ำ | ขึ้น "ชำระซ้ำไม่ได้" ทั้งที่จ่ายสำเร็จ → พนักงานอาจเก็บเงินซ้ำ | `client_request_id` ต่อการชำระ; ส่งซ้ำได้ผลเดิม (`idempotent`) |

| 7 | ตั้งเวลาโปรโมชันเป็นเวลาลาว แต่ระบบเทียบกับเวลา UTC | โปรเริ่มช้า/หมดเร็วไป 7 ชั่วโมง | แปลงเวลาเป็น UTC ตอนบันทึก และแปลงข้อมูลเก่าตอนเทียบ |
| 8 | รวมบิลเดลิเวอรีเข้าบิลอื่น ค่าส่งหายไป | ร้านเก็บเงินขาดเท่าค่าส่ง | ค่าส่งย้ายตามรายการไปบิลปลายทาง |
| 9 | SQLite: หลายมือถือสั่ง QR โต๊ะเดียวพร้อมกัน | เกิดหลายบิลในโต๊ะเดียว (พบ 4 บิลจาก 4 เครื่อง) | `_lock_tx()` บน SQLite ใช้ `BEGIN IMMEDIATE` |

ข้อ 5 และ 9 สำคัญต่อขั้น 2 เพราะ POS ในเครื่องร้านน่าจะใช้ SQLite

## Migration
- เพิ่ม `payments.shift_id`, `payments.client_request_id` และ index สองตัว
- รันซ้ำได้ ทดสอบกับฐานข้อมูล SQLite รุ่นเก่าแล้ว; Postgres ใช้ `ADD COLUMN IF NOT EXISTS`

## ผลการตรวจ
- SQLite: 30 ผ่าน, 3 ข้าม (เฉพาะ Postgres)
- PostgreSQL 16: 33/33 ผ่าน รวม backup/restore จริง
- เคสแข่งกันระหว่างสองเครื่อง: ผ่าน 10/10 รอบทั้งสองฐานข้อมูล
- round30, round31, customer_history JS: ผ่าน
- ปรับ `round16`/`round23` audit ให้ตรวจ helper ใหม่แทนข้อความเดิม
- `round22_ui_checkout_audit.py` ล้มอยู่แล้วก่อนรอบนี้ (ตรวจข้อความที่ถูกเปลี่ยนไปนานแล้ว) — ไม่ได้แก้

## ช่องว่างที่ปิดแล้ว (รอบ 2 — 11 เคสใหม่)
- ยกเลิกรายการ/ยกเลิกบิลหลังส่งครัว → สต็อกคืน, ยอดขายไม่นับ, รายงานนับการยกเลิก, บิลที่ยกเลิกแล้วจ่ายไม่ได้
- โปรโมชัน % + เพดานส่วนลด + ขั้นต่ำ + ภาษี; ช่วงเวลาโปรตามเวลาลาว (บั๊ก 7)
- ขอบวันเที่ยงคืนเวลาลาว (23:50 / 00:10 แยกวันถูก)
- ค่าส่ง delivery ในบิล รายงาน และลิ้นชัก; รวมบิลแล้วค่าส่งไม่หาย (บั๊ก 8)
- QR หลายคนโต๊ะเดียว → บิลเดียว → พนักงานเพิ่มรายการ → จ่าย → ปิดกะ; สั่งพร้อมกัน 4 เครื่อง (บั๊ก 9)
- ออเดอร์ออฟไลน์ซิงก์ซ้ำ/ซิงก์พร้อมกัน → บิลเดียว ตัดสต็อกครั้งเดียว → จ่าย → รายงาน

## ผลการตรวจรอบ 2 (เครื่อง Kot, SQLite)
- 44 ผ่าน, 3 ข้าม (เฉพาะ Postgres) — รันซ้ำ 5 รอบไม่มีล้ม
- round30, round31, customer_history JS, round16/23/28 audit: ผ่าน
- PostgreSQL: จะรันใน CI เมื่อเปิด PR เข้า main (เครื่องนี้ไม่มี Postgres)

## ติดตั้งใหม่
- ฐานข้อมูลใหม่สร้าง "สาขาหลัก" ให้อัตโนมัติ (เดิมสร้างร้านแต่ไม่มีสาขา → เพิ่มเมนูไม่ได้)
- พร้อมโต๊ะ 1–6 และเมนูตัวอย่าง 3 หมวด 10 รายการ (แก้ไข/ลบได้) — ร้านที่มีข้อมูลอยู่แล้วไม่ถูกแตะ
- `test_fresh_install_is_ready_to_sell`: ติดตั้งใหม่ → สั่ง → เปิดกะ → จ่าย → ยอดตรงทั้งรายงานและกะ

## ผลต่อพนักงาน
ถ้ายังไม่เปิดกะ จะรับชำระเงินไม่ได้ทุกช่องทาง หน้าชำระเงินจะแสดงช่องเปิดกะให้ทันที
