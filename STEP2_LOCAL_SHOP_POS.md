# ขั้น 2 — POS ที่ทำงานในเครื่องร้าน (ZaabOS Local)

## แนวทาง
ใช้แกนเดียวกับเวอร์ชันคลาวด์ (ไม่แยกโค้ด) แต่รันบนคอมแคชเชียร์เป็น "เซิร์ฟเวอร์ในร้าน"
- ฐานข้อมูล SQLite อยู่ในเครื่อง → ขาย/ส่งครัว/รับเงินได้แม้เน็ตล่ม
- แท็บเล็ต/มือถือพนักงานและ QR ลูกค้าเชื่อมผ่าน Wi-Fi ร้านมาที่เครื่องนี้
- แพ็กเป็นโปรแกรมดับเบิลคลิก: Windows (`ZaabOS.exe`) และ macOS (`ZaabOS.app`)

## ไฟล์
| ไฟล์ | หน้าที่ |
|---|---|
| `zaabos_local.py` | ตัวเปิดโปรแกรม: หาโฟลเดอร์ข้อมูล, รหัสเข้าครั้งแรก, สำรองอัตโนมัติ, เปิดเบราว์เซอร์, เซิร์ฟเวอร์ waitress |
| `zaabos_local.spec` | สูตรแพ็กโปรแกรม (PyInstaller) |
| `requirements-local.txt` | Flask + waitress (ไม่ต้องใช้ Postgres) |
| `.github/workflows/build-local-app.yml` | สร้างและทดสอบโปรแกรมบน Windows และ Mac จริงใน GitHub |
| `tests/test_local_mode.py` | ทดสอบแบบร้านจริง: เปิด → ขาย → ปิด → เปิดใหม่ ข้อมูลอยู่ครบ |

## โฟลเดอร์ข้อมูล
- Windows: `%APPDATA%\ZaabOS` · macOS: `~/Library/Application Support/ZaabOS`
- `zaabos.db` (ข้อมูลร้าน), `.secret_key`, `backups/`, `first-login.txt` (ลบเองหลังเปลี่ยนรหัสครั้งแรก)
- อัปเดตโปรแกรมใหม่ = ข้อมูลไม่หาย เพราะอยู่นอกโฟลเดอร์โปรแกรม

## สิ่งที่แก้ในแกนระบบ
| เรื่อง | ก่อน | หลัง |
|---|---|---|
| SQLite หลายเครื่อง | ล็อกทั้งไฟล์, รอ 5 วิแล้ว error | WAL + รอ 15 วิ: หน้าครัว/รายงานอ่านได้ระหว่างแคชเชียร์เขียน |
| สำรองข้อมูล SQLite | copy ไฟล์ตรง ๆ (อาจได้ไฟล์เสียระหว่างมีคนเขียน) | ใช้ SQLite backup API ได้สำเนาสมบูรณ์เสมอ |
| ที่เก็บสำรอง | โฟลเดอร์โปรแกรม (หายตอนอัปเดต) | ในโฟลเดอร์ข้อมูล |
| QR โต๊ะ | ใช้ที่อยู่ที่แคชเชียร์เปิด (`localhost` → มือถือลูกค้าเข้าไม่ได้) | ใช้ที่อยู่เครื่องในวง Wi-Fi (`ZAABOS_PUBLIC_URL`) |

## ผลการตรวจ
- ทดสอบทั้งชุด 47 ผ่าน (SQLite) รวม end-to-end ขาย → รีสตาร์ต → ยอด/กะ/สำรองยังอยู่
- โปรแกรมที่แพ็กแล้วบน Mac: เปิดได้, ล็อกอินได้, สร้างฐานข้อมูลและสำรองในโฟลเดอร์ข้อมูล
- Windows + Mac จริงใน GitHub Actions: ทดสอบ 34 ผ่านทั้งสองระบบ, โปรแกรมที่แพ็กแล้วเปิดได้ สร้างฐานข้อมูลและสำรองถูกที่
- บั๊กเฉพาะ Windows ที่เจอและแก้: ไม่มีฐานข้อมูลเขตเวลา (แพ็ก `tzdata`), อ่านไฟล์ schema ภาษาไทยด้วย codepage ผิด (บังคับ UTF-8), log ภาษาไทยทำ console ล่มได้

## ข้อจำกัดที่รู้แล้ว (ไปแก้ในขั้น 3–4)
- IP ของเครื่องแคชเชียร์เปลี่ยนได้ถ้าเราเตอร์แจก IP ใหม่ → QR ที่พิมพ์ไว้ใช้ไม่ได้ ต้องล็อก IP ที่เราเตอร์ (หรือใช้ชื่อเครื่องแทน IP)
- เบราว์เซอร์บนแท็บเล็ตที่เข้าผ่าน `http://192.168…` ไม่เปิดโหมดออฟไลน์ (service worker ต้องใช้ HTTPS) — ไม่กระทบถ้า Wi-Fi ร้านยังทำงาน
- Mac: โปรแกรมไม่มีหน้าต่างให้กดปิด (ปิดผ่าน Activity Monitor) — Windows มีหน้าต่างดำบอกให้เปิดทิ้งไว้
- โปรแกรมยังไม่ได้ sign → Windows/Mac จะเตือนตอนเปิดครั้งแรก
- ยังไม่มีการย้ายข้อมูลจากคลาวด์ (zaabos.com) ลงเครื่องร้าน

---

# ขั้น 3 (ส่วนแรก) — พิมพ์ตรงถึงเครื่องพิมพ์ Wi‑Fi (RP331)

- `printing.py`: วาดใบเสร็จ/ใบครัวเป็นภาพ (ไทย/ลาว/อังกฤษ) → ส่ง ESC/POS ทาง TCP 9100 → ไม่มีหน้าต่าง Print
- ภาษาลาว: Pillow บน Mac จัดวรรณยุกต์ลาวเองไม่ได้ → จัดตำแหน่งสระ/วรรณยุกต์เองใน `_draw_lao`
- ทุกใบเป็นงานในคิว: `pending → printed` หรือ `failed` หลังลอง 4 ครั้ง → แถบแดง "พิมพ์ไม่สำเร็จ" + พิมพ์ซ้ำครั้งเดียวแบบควบคุม
- ใบครัวจำรายการที่ส่งรอบนั้น (`item_ids`) → พิมพ์ซ้ำได้ใบเดิม, แยกเครื่องตามสถานีครัวได้
- คลาวด์ (zaabos.com) ไม่แย่งงานพิมพ์ — เบราว์เซอร์พิมพ์เหมือนเดิม
- ตั้งค่า: เมนู ตั้งค่าร้าน & ใบเสร็จ → การ์ด "เครื่องพิมพ์ในร้าน" → ใส่ IP → พิมพ์ทดสอบ

---

# ขั้น 3–4 — ความเสถียรของเครื่องร้าน (ข้อ 1–6 ที่ Kot อนุมัติ 2026-09-23)

| # | เรื่อง | ทำอย่างไร | ไฟล์ |
|---|---|---|---|
| 1 | Mac/PC ห้ามหลับ | `caffeinate -is` ผูกกับโปรเซส (Mac) · `SetThreadExecutionState` (Windows) | `local_ops.keep_awake` |
| 2 | เปิดเองตอนเปิดเครื่อง + เปิดกลับเมื่อล่ม | LaunchAgent `com.zaabos.local` (KeepAlive เฉพาะออกผิดปกติ) · Windows: Run key | `local_ops.set_autostart` |
| 3 | ที่อยู่แท็บเล็ต/QR | เตือนเมื่อ IP เปลี่ยน · โหมด "ชื่อเครื่อง" (`xxx.local`) · แนะนำล็อก IP ที่เราเตอร์ | `local_ops.resolve_public_url` |
| 4 | สำรองนอกเครื่อง + กู้ข้อมูล | คัดลอกทุก backup ไป iCloud Drive/OneDrive `ZaabOS Backups` (เก็บ 30) · กู้ข้อมูล: ตรวจไฟล์ → สำรองของเดิม → สลับตอนเปิดใหม่ | `local_ops`, `local_api` |
| 5 | อัปเดตอัตโนมัติ | tag `local-vX.Y.Z` → GitHub Release (zip Mac/Win) → แอปเช็ก/ดาวน์โหลด/สลับเอง ถ้าพังคืนตัวเก่า | `build-local-app.yml`, `local_ops.install_update` |
| 6 | ดึงเมนูจาก zaabos.com | ล็อกอินบัญชีคลาวด์ → คัดลอกหมวด/เมนู/ตัวเลือก/โต๊ะ(QR ใหม่)/สถานีครัว/ภาษี/ใบเสร็จ · ของเดิมเก็บเข้าคลัง | `local_api.import_from_cloud` |

ควบคุมได้ 2 ทาง: เมนู 🍜 บนแถบด้านบน (Mac) และ ตั้งค่าร้าน → "โปรแกรมในเครื่องร้าน" (ทุกเครื่อง รวม Windows)

## ออกเวอร์ชันใหม่
1. แก้ `APP_VERSION` ใน `local_ops.py` (เช่น 2.1.1) → commit
2. `git tag -a local-v2.1.1 -m "..." && git push origin local-v2.1.1`
3. CI สร้าง+ทดสอบ Mac/Windows → เผยแพร่ Release → เครื่องร้านเห็น "มีเวอร์ชันใหม่"

## ข้อจำกัดที่รู้
- MacBook: ปิดฝาจอ = เครื่องหลับ (caffeinate กันไม่ได้) → เปิดฝาไว้ หรือต่อจอนอก+ไฟ
- Windows: เครื่องพิมพ์ USB ยังไม่รองรับ (ใช้ Wi‑Fi) · ยังไม่มีเปิดกลับเองเมื่อล่ม
- โหมดชื่อเครื่อง: Android บางรุ่นเปิด `.local` ไม่ได้ → ค่าเริ่มต้นใช้ IP + ล็อก IP ที่เราเตอร์
