# ชุด B — อัปเดตแอปทดลอง ZaabOS เดิมจาก 2.7.0 เป็น 2.8.0

ชุดนี้ใช้แทนไฟล์แอป ZaabOS ที่คุณใช้ทดลองอยู่เดิม ไม่สร้างแอปแยกชื่อ Trial และไม่สร้างบัญชีใหม่

1. เลือก ZIP สำหรับ Mac ชิป M (AppleSilicon) หรือ Intel ตามเครื่อง แล้วแตกไฟล์
2. ในแอปเดิม กดสำรองข้อมูลตอนนี้ จากนั้นเลือกปิด ZaabOS จากเมนูด้านบน
3. นำ ZaabOS.app ที่ได้ไปแทนไฟล์แอปทดลองเดิมในโฟลเดอร์เดิม หากชื่อเดิมเป็น ZaabOS Local.app ให้เปลี่ยนชื่อไฟล์ใหม่ให้ตรงก่อนแทนที่
4. เปิดแอปเดิมอีกครั้ง เมนูเวอร์ชันต้องเป็น 2.8.0 ใช้บัญชีเดิมและข้อมูลทดลองเดิม

ข้อมูลแอปอยู่ที่ ~/Library/Application Support/ZaabOS/ ซึ่งอยู่นอกไฟล์ .app
ไฟล์ชุดนี้ไม่มีฐานข้อมูลหรือรหัสผ่าน และไม่ย้ายหรือลบโฟลเดอร์ข้อมูล

ฟีเจอร์ชุด B: หมายเหตุด่วน, ห่อกลับรายจาน, แก้ราคา/แถมพร้อมอนุมัติผู้จัดการ, ใส่ราคาตอนสั่ง และเร่งอาหาร
แก้บั๊กบันทึกตัวเลือกอาหารซ้ำเมื่อสร้างบิลและเพิ่มรายการในบิลแล้ว
ยังไม่รวมงานหลายสกุลเงิน

ใช้ชุดนี้เฉพาะแอปที่คุณระบุว่าเป็นตัวทดลอง ไม่ใช่ตัวเว็บ
การสร้างไฟล์ผ่านสาขาทดลองไม่รวมโค้ดเข้า main และไม่ออก GitHub Release สำหรับการอัปเดตอัตโนมัติ
จึงต้องติดตั้งไฟล์ชุดนี้ด้วยตนเอง ปุ่มตรวจหาอัปเดตเดิมจะไม่เห็นชุดทดลองนี้
